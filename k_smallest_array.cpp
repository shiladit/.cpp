#include<bits/stdc++.h>

using namespace std;


void find_k(int arr[],int size,int k){
	
}

int main(){
	
	int T;
	cin >> T;
	while(T){
		int size;
		cin >> size;
		int arr[size];
		for(int i=0;i<size;i++)
			cin >> arr[i] ;
		int k;
		cin >> k;

		find_k(arr,size,k);
		T--;

	}
}